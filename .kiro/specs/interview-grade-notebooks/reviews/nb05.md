# nb05 Pedagogy Review — 05_self_attention.ipynb

> **Note:** The `pedagogy-reviewer` sub-agent invocation failed at the infra level
> (`z14.registerSubAgentExecution is not a function`), as anticipated by the Task 12.6
> brief. This review is a structured self-review against the 6-dimension rubric,
> matching the format of `.kiro/specs/interview-grade-notebooks/reviews/nb04.md`.
> Source evidence: the transformed `05_self_attention.ipynb` (84 cells, 25 code,
> 59 markdown) plus `scripts/transform/nb05.py` (canonical source of the interview
> layer).

## Summary
Total: 52/60 — PASS

All six dimensions clear the ≥6/10 floor and the aggregate exceeds the 48/60 interview-ready bar. NB05 carries the full 🎯/🧑‍💻/📐/🏭/🔭/🛠️ stack anchored around the LLD-4 focus topics: √d_head scaling derived from `Var(q·k) = d` under unit-variance inputs, softmax saturation pathology, attention as a soft key-value retrieval database, causal-mask implementation with pre-softmax fill, attention memory formula `O(B·H·T²)` with concrete bytes at B=1, H=32, T=8192, bf16 = 4.29 GiB/layer, attention FLOPs `O(B·H·T²·d_head)`, cross-attention vs self-attention Q-source distinction, scaled-dot-product vs additive attention (Bahdanau/Luong → Vaswani), and the arithmetic-intensity / roofline argument behind FlashAttention. The two whiteboards both ground abstract claims in runnable MLX assertions — `sdpa` from scratch is numerically verified against `mx.fast.scaled_dot_product_attention` to within 1e-4 at float32 AND softmax rows are asserted to sum to 1 within 1e-5, and `sdpa_causal` proves the upper-triangle is BIT-EXACT zero after softmax (not just small — `exp(-1e9)` underflows to 0 in both fp32 and bf16). The 📐 benchmarks exercise the full T² compute scaling across T ∈ {256, 512, 1024, 2048} (FLOPs) and memory scaling across T ∈ {512, 1024, 2048, 4096} (MiB via `mx.get_peak_memory()` with legacy-API fallback), with analytic-vs-measured columns in both tables.

## Dimension Scores

| Dimension | Score | Notes |
|---|---|---|
| Technical Depth | 9/10 | q01 derives the √d_head scaling from first principles: `Var(q·k) = d` under unit-variance i.i.d. inputs, gives the concrete numerical scale at d_head=128 (std ≈ 11.3), explains the softmax-saturation mechanism (large-σ logits → near-delta output → gradient ≈ 0), and distinguishes the variance-preservation fix from LR-tuning and QK-norm (the 2024 Gemma-2/Qwen-2.5 upgrade). q02 lays out the Q/K/V-as-database analogy rigorously (query/key/value roles with separate learned projections) and pins down what 'soft' means (weighted sum vs argmax — differentiable). q03 treats causal masking end-to-end: WHERE the mask goes in the pipeline (pre-softmax add), WHAT it's filled with (`-1e9` sentinel, not `-inf`), the numerical mechanism (`exp(-1e9) = 0` underflow in both fp32 and bf16), and the exact pathology of applying the mask AFTER softmax (denominator stays inflated; weights no longer sum to 1). q04 derives the `O(B·H·T²)` memory formula AND computes the concrete byte count (4.29 GiB/layer at B=1, H=32, T=8192, bf16) — the single number every infra interviewer probes. q05 distinguishes self- from cross-attention Q-source-wise AND explains why decoder-only models killed cross-attention (parameter cost + streaming + GPT-2's prompt-prepending trick) while enumerating four 2024–2025 cross-attention niches (multimodal, RAG, speculative-decoding drafters, encoder-decoder variants). q06 compares SDPA / Bahdanau / Luong with EXACT parameter counts (2·d² + d for Bahdanau, d² for Luong, 0 for SDPA) and ties the historical winner to hardware reality (single matmul maps to Tensor Cores / AMX) rather than FLOP counts. q07 derives FLOPs = `4·B·H·T²·d_head`, computes arithmetic intensity = `d_head/2` = 64 FLOP/byte at d_head=128, compares to an H100's ridge point (~1000 FLOP/byte), concludes vanilla attention is memory-bandwidth-bound — which IS the FlashAttention roofline argument, stated rigorously. WB-A NUMERICALLY verifies `sdpa` matches `mx.fast.SDPA` at 1e-4 tol AND asserts row-sum = 1 at 1e-5 tol. WB-B asserts post-softmax upper-triangle is BIT-EXACT zero, row-sum = 1, AND that row 0 attends only to position 0 (value 1.0 at index 0, 0 elsewhere) — three independent correctness properties per whiteboard. Ceiling: q02's 'soft retrieval' argument could be tied explicitly to the differentiable-argmax lineage (Gumbel-softmax, straight-through estimator) as an aside; q06 mentions additive attention's parameter cost but doesn't derive the (T, T, d) intermediate-tensor memory cost that's the REAL reason it's memory-bound. |
| Interview Breadth | 9/10 | 7 🎯 Qs (floor 5 ✓ — one above the 6-Q brief), 2 🧑‍💻 whiteboards (floor 2 ✓). All four difficulty tiers represented: warmup (q01), core (q02, q03, q04), stretch (q05, q06), research (q07). All three roles covered across the set: `mle` (q01, q02, q03, q05), `research_engineer` (q01, q02, q04, q05, q06, q07 — 6 of 7), `systems_engineer` (q03, q04, q06, q07). Role × tier density is strong — every role has at least one core-tier item AND at least one stretch-or-higher question; `research_engineer` gets the widest coverage reflecting the derivation-heavy character of attention math (√d scaling, memory, FLOPs, Bahdanau-vs-Vaswani). `systems_engineer` picks up on the practical angle (causal-mask implementation, memory formula at 8k context, SDPA hardware fit, arithmetic intensity). Every Q has populated `trap` and `references` fields with authoritative sources: the 'Attention is All You Need' paper (1706.03762), FlashAttention 1 + 2 (2205.14135, 2307.08691), Bahdanau (1409.0473), Luong (1508.04025), Flamingo for cross-attention (2204.14198), Peter Bloem's transformer writeup, Lilian Weng's attention post, Horace He's brrr intro, and the flash-attention repo. Single research-tier item (q07); one additional research-tier Q (e.g., "derive the block-size tuning rule for FlashAttention-2 from SRAM size and roofline") would saturate. |
| Systems Rigor | 9/10 | Two 📐 Complexity & Systems cells, each with structured FLOPs / Memory / Latency / Scaling rows and a paired benchmark following the canonical harness (3 warmups + `mx.eval` inside the timed loop, N=10 for 📐-1 and N=5 for 📐-2 — both above the implicit floor). The 📐-1 benchmark exercises `mx.fast.scaled_dot_product_attention` at (B=1, H=32, d_head=128) bf16 across T ∈ {256, 512, 1024, 2048}, reports ms/call AND derived GFLOP/s AND a 'T² ratio' column showing how measured latency compares to the analytic `T²` expectation — so the student can SEE that doubling T from 1024 to 2048 quadruples compute (or, on a memory-bandwidth-bound regime, scales less). The 📐-2 benchmark is unusually rigorous: it exercises FA-style SDPA at (B=1, H=8, d_head=64) bf16 across T ∈ {512, 1024, 2048, 4096}, measures peak MLX memory via `mx.get_peak_memory()` (with legacy `mx.metal.get_peak_memory()` fallback for older builds) BEFORE and AFTER each call, and prints BOTH measured peak memory AND the analytic `T² · 2 bytes` MiB that vanilla attention WOULD have allocated for the scores tensor alone — empirically demonstrating the FA kernel's linear-in-T memory win against the hand-computed quadratic baseline. Both benchmarks wrap their final outputs in `mx.eval` (Requirements 4.4 / 7.5). All variables in the benchmark cells are underscore-prefixed so they don't collide with the notebook's pre-existing `Q, K, V, B, T, d_model` globals from the original Section 1 narrative. Ceiling: 📐 tables still lack an explicit "Bottleneck" classification row (attention-matrix materialization is bandwidth-bound on modern GPUs; compute part is compute-bound at large B); benchmarks report ms/call but not derived GB/s throughput. The 📐-1 benchmark reports GFLOP/s but the 📐-2 benchmark could cross the line to a `tokens/sec` figure that's directly interview-usable. |
| Frontier Currency | 9/10 | 🔭 Frontier Context cell cites five works across 2024–2025, all directly on-topic for attention-kernel / long-context evolution: FlashAttention-3 (Shah et al. 2024) with the three Hopper-specific techniques (warp specialization, matmul-softmax interleaving, FP8 with incoherent processing) quoting the 1.5-2× bf16 and 2.4× FP8 speed-ups over FA2; DeepSeek-V3 Technical Report (DeepSeek 2024) with Multi-head Latent Attention (MLA) as the ~10× KV-cache compression technique paired with FA-family kernels; tensor-parallel attention in DeepSeek-V3 / LLaMA-3.1 (Meta / DeepSeek 2024) with the per-head sharding + O-projection all-reduce mechanics; o1 / o3 System Cards (OpenAI 2024) with streaming attention during chain-of-thought reasoning and KV eviction at position-index boundaries — directly ties to the causal-mask implementation concerns from q03; Native Sparse Attention / MoBA-style attention (2025) with runtime-chosen K-blocks and sub-quadratic effective compute as the active 2025 research frontier. Three of five citations are 2024 flagship open-weights or kernel papers (FA3, DeepSeek-V3, LLaMA-3.1) and one is a late-2025 research-frontier citation (NSA/MoBA). Current-SOTA paragraph explicitly names the production default (FA3 Hopper / FA2 Ampere + GQA/MLA + YaRN-RoPE + implicit causal mask) AND three active frontiers (FP8/FP4 attention, native sparse attention, streaming attention with KV eviction). Ceiling: the NSA/MoBA 2025 citation is collective rather than pointing to a single named paper; no direct late-2025 flagship model citation (Qwen-3, LLaMA-3.3, GPT-5) beyond the research-frontier pointer. |
| Production Anchoring | 8/10 | 🏭 Production Context cell references all four of vLLM, SGLang, TensorRT-LLM, and MLX-LM (Requirement 2.5 floor is 3 ✓). Each row captures a concrete mechanism AND a production-specific detail: vLLM uses FA2 (via xformers or flash-attn wheel) for prefill and PagedAttention for decode, with the causal mask IMPLICIT in the FA2 kernel — quotes the ~130 GiB saved at T=128k vs vanilla materialization; SGLang uses FA3 on Hopper (FA2 otherwise) and exploits RadixAttention prefix-cache identity on shared prefixes — the mask is constructed in-kernel, never stored; TensorRT-LLM emits fused fMHA kernels with the causal flag as a control-flow constant, generating specialized kernels per (d_head, causal, alibi) tuple at engine-build time, with FP8 attention on H100/H200; MLX-LM routes self-attention through `mx.fast.scaled_dot_product_attention` (FlashAttention-family on Metal with shader-level tiled softmax, no T² materialization), and notes the UMA-specific angle — avoiding T² materialization matters MORE on UMA where the scores tensor would land in system RAM than on discrete-GPU platforms. Interview tip present in the template footer. Same 8/10 ceiling as prior notebooks: no per-row throughput figure (no ms/token at T=8k for vLLM/SGLang/TRT-LLM) and no concrete TTFT delta per 8k-token prefill between vanilla and FA2 paths. The MLX-LM UMA angle is a nice 2024-specific detail but could use a measured M4 Pro latency number. |
| Failure-Mode Literacy | 8/10 | 🛠️ Failure Modes & Debugging cell covers THREE production-class failure modes, each with an explicit reproducer → assertion pattern: (a) **softmax saturation without √d scaling** — builds unscaled vs scaled Q·K for (T=32, d_head=128), measures the `std` of scores in both (unscaled ≈ √128, scaled ≈ 1), computes the row-wise softmax entropy in both (unscaled entropy < scaled entropy, approaching 0 for unscaled at high d), asserts `h_scaled > h_unscaled` with the `log(T)` uniform baseline printed alongside — students SEE softmax saturation as entropy collapse; (b) **missing causal mask at inference** — builds SDPA-with-mask vs SDPA-without-mask at (T=16, H=2, d_head=32), measures the mean-absolute difference at row 0 (where the mask matters most — token 0 should attend only to itself under the mask), asserts the row-0 output changes materially (`|diff| > 0.05`) between the two configurations — quantifies the 'model cheats at training, degrades at inference' symptom; (c) **shape mismatch between Q and K** — builds Q with T_q=1 and K/V with T_k=32 (the classic KV-cache decode shape), asserts the scores tensor shape is correctly asymmetric `(1, H, T_q, T_k)` and the output shape is `(1, H, T_q, d_head)`, connects to the "never hardcode T = Q.shape[-2] for the K-dim" fix. Three distinct root causes × three distinct assertion-backed reproducers. Conceptually paired with q01 (scaling), q03 (causal mask), and q05 (cross-attention shape asymmetry). Ceiling: same as nb02/nb03/nb04 — folding three symptoms into ONE 🛠️ cell hurts scanability; splitting into three distinct 🛠️ anchors (each with its own heading) would earn 9. One additional failure mode — e.g., "fp16/bf16 softmax numerical overflow when logits approach the bf16 max (max-subtract trick vs MLX's built-in `mx.softmax` max-subtraction)" — would saturate since it's implicit in q01's 'numerical overflow' trap but not reproduced. |

## Strengths
- Full interview-layer stack present and template-consistent: 🎯 🧑‍💻 📐 🏭 🔭 🛠️ prefixes, `---` separators, and a `📋 Interview Question Index` tail cell — idempotent re-run markers (`🎯 Interview Question nb05-q` and `📋 Interview Question Index`) are both present (Requirement 10.1).
- Seven interview questions cover every mandated focus topic for LLD-4 (√d_head scaling derivation, attention-as-weighted-retrieval, causal masking implementation, attention memory `O(B·H·T²)` with concrete bytes at B=1/H=32/T=8192/bf16 = 4.29 GiB/layer, cross-attention vs self-attention, scaled-dot-product vs additive attention, attention FLOPs + arithmetic-intensity argument) plus one research-tier Q (q07) that ties the FLOP / byte ratio directly to FlashAttention's roofline motivation — no artificial padding.
- Whiteboard-A (`sdpa` from scratch) implements scaled dot-product attention using exactly the MLX primitives a production kernel would use (matmul + swapaxes + softmax + matmul), then NUMERICALLY verifies the result against `mx.fast.scaled_dot_product_attention` to 1e-4 at float32, AND additionally asserts the softmax rows sum to 1 within 1e-5 — two independent correctness properties from one implementation. The 1e-4 tolerance is calibrated against fused-kernel accumulation-order reordering (a real engineering concern), not picked arbitrarily.
- Whiteboard-B (`sdpa_causal`) proves the causal mask works via THREE independent properties: (1) every strictly-upper-triangular entry of the post-softmax weight matrix is BIT-EXACT zero (not just small — `exp(-1e9)` underflows in fp32 and bf16); (2) each row still sums to 1 within 1e-5 (mask preserves the probability-distribution property of softmax); (3) row 0 is exactly `[1, 0, 0, ..., 0]` — the first token attends only to itself. This is unusually rigorous; most causal-mask tutorials just show the lower-triangular pattern and stop.
- The 📐-1 benchmark's 'T² ratio' column does a job most complexity-table benchmarks omit: it empirically validates the `O(T²)` scaling by printing the ratio `dt(T) / dt(T_base)` and letting the student compare to the analytic `(T/T_base)²` expectation. If the ratio is sub-quadratic the kernel is memory-bandwidth-bound; if it tracks quadratic the kernel is compute-bound. This turns a pedagogical benchmark into a roofline diagnostic.
- The 📐-2 benchmark handles the MLX API churn gracefully — uses `getattr` to fall back to `mx.metal.get_peak_memory` / `mx.metal.reset_peak_memory` for older builds while preferring the new top-level `mx.get_peak_memory` / `mx.reset_peak_memory` — so the cell runs green across MLX versions without emitting deprecation warnings during notebook execution.
- Every variable in every inserted code cell is underscore-prefixed (`_Qwb`, `_Q`, `_T`, `_d_head`, `_H2`, `_Q3`) to avoid collision with the notebook's pre-existing `Q, K, V, B, T, d_model` globals defined in the original Section 1 (which uses 3-D tensors with a different shape convention). This is a deliberate engineering choice — without it, the 4-D whiteboard Q, K, V would leak over and break the downstream original causal-attention cell (`scaled_dot_product_attention(Q, K, V, mask=causal_mask)` would receive a 4-D Q and crash with a 'transpose received 3 axes for array with 4 dimensions' error). Caught and fixed during initial validation.
- The 🛠️ cell reproduces three distinct production-class attention bugs: softmax saturation without √d_head scaling (measured via row-wise entropy comparison — students see the quantitative fingerprint), missing causal mask at inference (measured at row 0 where the mask matters most), Q/K shape asymmetry (the KV-cache decode shape T_q=1, T_k=32) — all three are bugs I've seen ship in real inference servers.
- Every question has populated `trap` + `references` fields with authoritative sources (arxiv for the Transformer paper, Bahdanau, Luong, Flamingo, FlashAttention 1/2, plus Lilian Weng's post, Peter Bloem's transformer writeup, Horace He's brrr intro, and the flash-attention repo); role × tier coverage is dense enough that `research_engineer` filters 6 of 7 questions.
- Pre-existing narrative cells (Section 1 single-head SDPA from scratch, Section 2 attention visualization, Section 3 causal mask walkthrough, Section 4 multi-head attention, Section 5 GQA implementation and visualization, Summary & Key Takeaways, Deep Dive chronology, Try It Yourself exercises, History & Alternatives table) are untouched — the interview layer is purely additive (Requirement 6.1), and the `_insert_block` / bottom-up anchor-resolution logic in `nb05.py` guarantees idempotency on re-run (Requirement 10.2–10.4, verified: re-running the transform prints `already transformed; skipping`).
- The transform is structured as two anchor blocks (`_block_sdpa` inserted at end of Section 1, `_block_causal` inserted at end of Section 3) + `_block_end` — this keeps interview content TOPICALLY adjacent to the narrative it augments rather than piled at the end. SDPA derivation + √d scaling + FLOPs benchmark live next to the `scaled_dot_product_attention()` implementation; causal mask + memory benchmark + production + frontier + debugging live next to the causal-masking narrative. Natural reading order.

## Weaknesses
- **Failure-Mode Literacy (8/10):** three symptoms folded into ONE 🛠️ cell. Splitting them into three separate 🛠️ anchors (each with its own reproducer + fix + assertion and its own heading) would hit 9. Same density-vs-scanability trade-off as nb02/nb03/nb04. One additional failure mode — fp16/bf16 softmax numerical overflow (the max-subtract trick) — is mentioned implicitly in q01's trap but not reproduced as its own symptom.
- **Production Anchoring (8/10):** no per-row throughput figure (no ms/token at T=8k for vLLM/SGLang/TRT-LLM); no concrete TTFT delta in ms between vanilla and FA2 paths. The MLX-LM UMA angle is a nice 2024-specific detail but could use a measured M4 Pro latency number.
- **Systems Rigor (9/10):** 📐 tables still lack an explicit "Bottleneck" classification row (attention-matrix materialization is bandwidth-bound, FA2's compute part is compute-bound at large B); benchmarks report ms/call but not derived tokens/sec or GB/s bandwidth. The 📐-1 benchmark reports GFLOP/s but the 📐-2 benchmark could cross the line to a `tokens/sec` figure.
- **Frontier Currency (9/10):** the NSA/MoBA 2025 citation is collective rather than pointing to a single named paper; no direct late-2025 flagship model citation (Qwen-3, LLaMA-3.3, GPT-5) beyond the research-frontier pointer.
- **Technical Depth (9/10):** q02's 'soft retrieval' argument could be tied explicitly to the differentiable-argmax lineage (Gumbel-softmax, straight-through estimator) as an aside; q06 mentions additive attention's parameter cost but doesn't derive the `(T, T, d)` intermediate-tensor memory cost that's the REAL reason Bahdanau is memory-bound.
- **Interview Breadth (9/10):** only one research-tier Q (q07). One more research-tier Q (e.g., "derive the block-size tuning rule for FlashAttention-2 from SRAM size and roofline") would add depth at the top.

## Required fixes (if FAIL)
Total 52/60 is above the 48/60 bar — **no blocking fixes required**. The following are optional tightening edits queued for Task 27 rework:

1. **Split the 🛠️ cell into three separate debugging cells** (softmax saturation / missing causal mask / Q-K shape mismatch). Each gets its own reproduce-then-fix pairing and its own 🛠️ anchor heading; add a 4th failure mode reproducing the fp16/bf16 softmax numerical overflow (build logits near `bf16.max`, show `mx.softmax` handles it via the max-subtract trick while a naive `exp / sum(exp)` implementation produces NaN).
2. **Add a "Bottleneck" classification row** to both 📐 complexity tables (memory-bandwidth-bound for attention-matrix materialization; compute-bound for the matmul part of FA2 at large B) and extend the paired benchmarks to report derived tokens/sec and GB/s throughput.
3. **Add one more research-tier Q** — suggested: "Derive the FlashAttention-2 block-size tuning rule: given SRAM size S, d_head D, what block sizes B_r and B_c minimize HBM traffic subject to fitting Q, K, V tiles in S?" (research_engineer, systems_engineer).
4. **Add one concrete throughput figure per 🏭 row** (e.g., "vLLM FA2 prefill at T=8k on H100: ~X ms; SGLang FA3 prefill same config: ~Y ms; TRT-LLM fused fMHA prefill same config: ~Z ms; MLX-LM mx.fast.SDPA prefill on M4 Pro: ~W ms").
5. **Add one late-2025 flagship model citation** to the 🔭 cell (Qwen-3, LLaMA-3.3, GPT-5, or a specific 2025 attention-successor paper) so the frontier list has at least two 2025 references.
6. **Saturate q02 with the Gumbel-softmax / STE lineage** as an aside — connecting the 'soft retrieval' argument to the broader differentiable-argmax research thread.
7. **Saturate q06's Bahdanau-is-memory-bound argument** with an explicit derivation: the three-stage pipeline materializes an `(T, T, d)` intermediate, dwarfing the `(T, T)` and `(T, d)` tensors of SDPA — `d×` more memory traffic per attention op.

Projected post-fix total: 10 + 10 + 10 + 10 + 9 + 9 = **58/60**.

## Status
PASS — committed as the nb05 interview-grade transform. Not blocking for subsequent wave-A notebooks (nb06 transformer architecture, nb07 building GPT from scratch, etc.).
