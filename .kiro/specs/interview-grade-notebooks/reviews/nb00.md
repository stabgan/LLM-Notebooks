# nb00 Pedagogy Review — 00_environment_apple_silicon.ipynb

## Summary
Total: 46/60 — FAIL

All six dimensions clear the ≥6/10 floor, but the aggregate is 2 points short of the 48/60 interview-ready bar. Two surgical fixes in Systems Rigor and Frontier Currency will push this over the line without touching the beginner narrative.

## Dimension Scores

| Dimension | Score | Notes |
|---|---|---|
| Technical Depth | 7/10 | Roofline derivation in q04 (arithmetic intensity ≈ 2 FLOP/byte, crossover ≈ 18 FLOP/byte) is first-principles. KV-cache math in q05 is explicit (`2·L·T·n_kv·d_head·bytes`) with fp16/fp8/int4 max-param reasoning. Whiteboard 2 pairs the closed-form formula with three verifiable regimes. Weakness: most "derivations" are applied-arithmetic on hardware constants rather than symbolic derivations — acceptable for a Foundations-tier environment notebook but caps the ceiling. |
| Interview Breadth | 9/10 | 6 Interview Qs (floor 5 ✓), 2 Whiteboard Challenges (floor 2 ✓). All four difficulty tiers present: warmup (q01), core (q02, q03, q04), stretch (q05), research (q06). All three roles covered across the set: `mle` (q01, q02, q05), `research_engineer` (q02, q04, q06), `systems_engineer` (q03, q04, q05, q06). Trap fields and references are populated on every question. Only knock: research-tier representation is a single question — duplicating the pattern isn't strictly required but one more stretch/research item on e.g. power-budget arithmetic would make coverage feel less single-threaded. |
| Systems Rigor | 7/10 | Two Complexity & Systems cells present, each with FLOPs, memory, and latency rows and a paired benchmark using `time.perf_counter` + `mx.eval` + 3 warmups. However: **(a) the "Bottleneck" classification row required by Requirement 5.4 is missing from both complexity tables** — the bottleneck is stated only in surrounding prose, not in the structured row. (b) The "memory-bandwidth-upper-bound-calculator" benchmark times pure Python arithmetic (0.005 ms / call) rather than an actual bandwidth-bound MLX operation, so the headline claim "≈19.5 tok/s fp16 LLaMA-7B @ M4 Pro" is not empirically corroborated in-notebook. A real `mx.sum` over a large array or a matmul sweep would close the loop. |
| Frontier Currency | 7/10 | Frontier Context cell cites three works in the 2023–2025 window: MLX framework (2023), Gemma 4 on Apple Silicon (2025), community M4 Pro/Max benchmarks (2024). Late-2025 SOTA note covers M4 Max and M3 Ultra, and `arxiv 2309.06180` (vLLM / PagedAttention) appears in q04 references. Gap: no direct LLaMA-3 / Mistral / DeepSeek-V3 reference and no MLX release-note link anchoring the int4 quantization claim; a fourth 2024–2026 citation would strengthen this dimension without bloat. |
| Production Anchoring | 8/10 | Production Context cell references all four required systems (vLLM, SGLang, TensorRT-LLM, MLX-LM — exceeds the Requirement 2.5 floor of three) with a concrete mechanism and trade-off per row. vLLM row correctly names `cudaMemcpy` + pinned host buffers + PagedAttention; MLX-LM row correctly calls out `mx.load` + `mx.quantize` + KV-cache co-residency. Interview tip is crisp. Could add a throughput/cost number per system to hit 9/10. |
| Failure-Mode Literacy | 8/10 | 🛠️ cell is textbook: three root causes ranked by frequency (missing `mx.eval`, fp64 fallback, implicit CPU device), runnable diagnostic that reproduces the symptom (no-eval timing reports 0.09 ms vs 5.60 ms for the real matmul), fp32→bf16 comparison as a secondary fix, and a sanity assertion (`assert dt_no_eval_ms < t_fp32_ms`) that encodes the diagnostic invariant. Paired conceptually with q02 (core, mle+RE). One more failure mode — e.g., thermal throttling on a fanless Mac mini — would earn the 10. |

## Strengths
- Full interview layer present and template-consistent: 🎯 🧑‍💻 📐 🏭 🔭 🛠️ prefixes, `---` separators, and a `📋 Interview Question Index` tail cell.
- Question-bank coverage is textbook — every difficulty tier and every role represented across 6 questions.
- The 🛠️ debugging cell is genuinely reproducible: it reports the "lazy-eval trap" as a real number and asserts the pathological ordering.
- q04 (roofline) and q05 (memory budget at int4/fp8/fp16 with KV-cache headroom) are both strong, concrete systems-engineering questions with correct arithmetic.
- Production Context cell references all four required systems with accurate mechanism descriptions (not just brand-drops).
- Whiteboard challenges both include `mx.eval` before assertions and `assert` statements on shape and values, satisfying Requirement 4.

## Weaknesses
- **Systems Rigor (7/10):** both 📐 complexity tables omit the "Bottleneck" row (required by Requirement 5.4); the decode-bandwidth benchmark times Python arithmetic rather than a bandwidth-bound MLX op, so the tokens/sec claim lacks an empirical anchor in-notebook.
- **Frontier Currency (7/10):** only three citations in the 📚 frontier cell; missing a 2024–2026 flagship model (LLaMA-3, Mistral-Large, DeepSeek-V3) or an MLX release note to anchor int4 quantization claims.
- **Technical Depth (7/10):** hardware-topic ceiling — applied arithmetic dominates; acceptable for NB00 but not outstanding.
- **Interview Breadth (9/10):** single research-tier question; one additional stretch/research item would make role×difficulty coverage denser.

## Required fixes (if FAIL)

Two edits land ≥2 points and clear the 48/60 bar:

1. **Add the "Bottleneck" row to both 📐 complexity tables** (spec-compliance fix; Requirement 5.4)
2. **Replace the no-op "memory-bandwidth-upper-bound-calculator" benchmark with a real bandwidth probe** that times `mx.sum` over a large fp32 array with 3 warmups + 10 timed iterations, then reports measured GB/s.
3. **Strengthen the 🔭 frontier cell** — add one 2024–2026 reference (LLaMA-3 technical report, MLX 0.18+ release notes, or similar) with a one-line contribution summary.

After fixes 1 + 2 + 3: projected total = 7 + 9 + 9 + 8 + 8 + 8 = **49/60 PASS**.
