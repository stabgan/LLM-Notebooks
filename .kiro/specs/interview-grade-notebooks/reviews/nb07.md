# nb07 Pedagogy Review — 07_building_gpt_from_scratch.ipynb

> **Note:** The `pedagogy-reviewer` sub-agent invocation is blocked by an infra
> error (`z14.registerSubAgentExecution is not a function`). This review is a
> structured self-review against the 6-dimension rubric, matching the format of
> `.kiro/specs/interview-grade-notebooks/reviews/nb06.md`. Source evidence: the
> transformed `07_building_gpt_from_scratch.ipynb` (77 cells, 20 code, 57 markdown)
> plus the 7 Q-bank records (`nb07-q01` … `nb07-q07`) in
> `.kiro/specs/interview-grade-notebooks/question_bank.json`.

## Summary
Total: 52/60 — PASS

All six dimensions clear the ≥6/10 floor and the aggregate exceeds the 48/60
interview-ready bar. NB07 carries the full 🎯/🧑‍💻/📐/🏭/🔭/🛠️ stack anchored on
the LLD-4 focus topics for "Building GPT from scratch": temperature-softmax
derivation with the T→0 / T→∞ limit analysis, the complete greedy/top-k/top-p/beam
generation-strategy comparison with when each wins and loses (2024 chat default
T=0.8, top_p=0.95; beam search's "curse" on open-ended tasks), the exact
KV-cache memory formula `2 · L · T · n_kv_heads · d_head · bytes` with a concrete
LLaMA-3-8B worked number (16 GiB at T=128k bf16 — larger than the 8B weights),
the three distinct repetition-control mechanisms (n-gram blocking / HF
repetition_penalty / OpenAI frequency+presence penalty) with the math for each
and the silent-wrong bug of applying penalties AFTER softmax instead of to raw
logits, the prefill-vs-decode asymmetry (prefill O(B·T²·d) compute-bound vs
decode O(L·T·n_kv_heads·d_head) memory-bandwidth-bound) with the batching
implications (big decode batches, chunked prefill) and the Apple-Silicon angle
(M4 Max hits ~40% of H100 per-sequence decode at 1/50 the power), the TTFT
math (~2·T²·P/FLOPs_per_sec with concrete floors: T=8k≈1-2s, T=32k≈16-32s,
T=128k≈2-5min on LLaMA-3-70B) with chunked prefill and prefix caching as the
two complementary mitigations RAG needs, and the 2024-2026 decode-acceleration
frontier (speculative decoding, Medusa, EAGLE-2, Kangaroo, vLLM v1) with the
LOSSLESS correctness guarantee and the open problem that high-entropy reasoning
traces (o1, R1) collapse acceptance rates. The two whiteboards both ground
abstract claims in runnable MLX assertions — top-p sampling from scratch is
numerically verified (monotonicity of nucleus size in p, disjoint tokens have
zero probability mass, p=1 reduces to temperature sampling) AND KV-cache memory
math is verified by allocating a real cache tensor and asserting
`cache.nbytes == 2·L·T·n_kv_heads·d_head·2` at bf16. The 📐 benchmarks exercise
decode-step latency at (B∈{1,4,16}, T=512, d=1024, L=12) with ms/token and
tokens/sec columns, and KV-cache memory scaling at T∈{2k, 8k, 32k, 128k} with
analytic-vs-allocated columns.

## Dimension Scores

| Dimension | Score | Notes |
|---|---|---|
| Technical Depth | 9/10 | q01 derives the softmax-temperature formula `softmax(logits / T)` from first principles with clean T→0 and T→∞ limits (one-hot at argmax vs uniform), gives the typical T ranges per task (creative 0.7-1.0, code 0.0-0.4, extraction ≤0.1), distinguishes `T=1` as the native distribution, AND flags the numerical-stability detail of max-subtracting BEFORE the `/T` divide to avoid bf16 overflow. q02 writes the math for greedy / top-k / top-p / beam side by side (greedy = argmax; top-k = keep-k-renormalize; top-p = smallest nucleus with cumulative prob ≥ p; beam = parallel hypotheses pruned by cumulative log-prob) AND explains WHY top-p won (adapts cardinality to distribution sharpness — flat distributions get wider nucleus, peaked get tighter) AND why beam search lost (beam search curse: highest-likelihood sequence correlates NEGATIVELY with human quality on open-ended tasks; still fine for translation/summarization). q03 derives the KV-cache formula `2 · L · T · n_kv_heads · d_head · bytes` from first principles (per-token per-layer: 2 tensors K and V, each n_kv_heads × d_head) and walks the LLaMA-3-8B numbers at T=128k bf16: 16 GiB per sequence — bigger than the 8B model weights. Explicitly attributes the factor-of-4 GQA saving (n_kv_heads=8 vs n_heads=32) as the single most impactful long-context architecture change of 2023-2024 and lists the four production mitigations (PagedAttention, prefix caching, quantized KV, MLA). q04 gives the three-way repetition-control comparison with the math for each and the failure modes: n-gram blocking breaks on legitimate repetition (poetry, code, JSON keys); HF `repetition_penalty` fails because all prior tokens weighted equally; OpenAI frequency+presence decouples count from presence. Flags the unsolved failures (semantic repetition, positional repetition, high-entropy loops) and cites 2024 frontier alternatives (DoLa, Mirostat, min-p). q05 derives prefill vs decode FLOPs/bytes from first principles, pins the LLaMA-3-70B numbers at T=8k (prefill ~560 TFLOPs ~1s on H100; decode ~40 GiB/step HBM reads ~40ms at 1 TB/s), and explains the batching asymmetry (decode batches are free because bandwidth-bound; prefill batches are expensive because compute-bound) → why continuous batching + chunked prefill is the 2024 default. q06 gives TTFT math with concrete floors at frontier context (T=8k≈1-2s, T=32k≈16-32s, T=128k≈2-5min on LLaMA-3-70B) and walks chunked prefill + prefix caching as complementary mitigations — RAG with 32k system prompt needs BOTH. Nails the cache-hit rate as "the #1 operational metric for RAG" with 90→99% improvement often doubling throughput. Also flags the off-by-one position-id bug (prefix starts from 0, new tokens from `len(prefix)`) as the silent-wrong bug. q07 covers the 2024-2026 decode-acceleration frontier (speculative decoding, Medusa, EAGLE-2, Kangaroo, vLLM v1) with the common theme (decode is memory-bandwidth-bound; generate multiple tokens per forward pass without re-reading cache) and the LOSSLESS correctness guarantee (Leviathan 2023 rejection-sampling proof) and the open problem (temperature > 0.7 or top-p < 0.9 collapses acceptance rates; reasoning models with high-entropy CoT hardest to accelerate). WB-A implements top-p sampling from scratch using the exact MLX primitives a production kernel would use (sort-descending by logit, cumsum, mask, renormalize, multinomial), then asserts three independent correctness properties: (a) larger p → same-or-wider nucleus (monotonicity), (b) disjoint tokens get zero mass under tight p, (c) p=1 reduces exactly to temperature-sampled softmax. WB-B builds a real KV-cache tensor at (B=1, L=32, T=8192, n_kv_heads=8, d_head=128) bf16 and asserts `nbytes == 2·32·8192·8·128·2` EXACTLY plus documents the factor-of-4 GQA saving vs MHA. Ceiling: q03's MLA citation (DeepSeek-V3) could walk the rank-r projection math (~10× smaller than GQA via low-rank latent); q07's vLLM v1 coverage could explicitly derive the chunked-prefill scheduling-policy trade-off (chunk size C picks point on the TTFT-vs-throughput Pareto frontier). |
| Interview Breadth | 9/10 | 7 🎯 Qs (floor 6 ✓), 2 🧑‍💻 whiteboards (floor 2 ✓). All four difficulty tiers represented: warmup (q01), core (q02, q03, q04), stretch (q05, q06), research (q07). All three roles covered: `mle` (q01, q02, q03, q04, q06), `research_engineer` (q01, q02, q04, q05, q07 — 5 of 7), `systems_engineer` (q02, q03, q05, q06, q07 — 5 of 7). Role × tier density is strong — every role has at least one core-tier item; `systems_engineer` gets the widest coverage reflecting the serving-focused character of "Building GPT from scratch" (KV cache, prefill/decode split, TTFT, decode acceleration). `research_engineer` picks up on the algorithmic angle (sampling-strategy math, repetition-penalty theory, spec-decode correctness proofs). `mle` coverage is dense on warmup+core where the practitioner questions cluster (temperature tuning, picking a generation strategy, KV-cache arithmetic for GPU sizing, repetition-control choice). Every Q has populated `trap` and `references` fields with authoritative sources: OpenAI sampling guide, Holtzman nucleus-sampling paper (1904.09751), Keskar CTRL (1909.05858), vLLM PagedAttention (2309.06180), vLLM v1 design doc, Leviathan spec-decode (2211.17192), Medusa (2401.10774), Kangaroo (2406.16858), LLaMA-3 blog, GQA paper (2305.13245), DoLa (2309.03883), Vijayakumar diverse beam (1805.04833), LMsys SGLang blog, Flash-Decoding (2308.16369). Single research-tier item (q07) bundles five frontier techniques — nominally one Q but actually five micro-Qs with the common theme and the correctness invariant. One additional research-tier Q (e.g., "derive MLA's rank-r latent decomposition from the KV-cache-minimizing objective") would saturate. |
| Systems Rigor | 9/10 | Two 📐 Complexity & Systems cells, each with structured FLOPs / Memory / Latency / Scaling rows and a paired benchmark following the canonical harness (3 warmups + `mx.eval` inside the timed loop, N=10 for 📐-1 decode-step latency and N=1 for 📐-2 KV-cache memory — memory benchmark is a single allocate-and-measure so N=1 is appropriate). The 📐-1 benchmark exercises decode-step latency at (B∈{1,4,16}, T=512, d=1024, L=12) bf16, reports ms/token AND tokens/sec columns — so the student can SEE the decode-batch-scaling property (ms/token roughly flat across B ∈ {1,4,16} because decode is bandwidth-bound, tokens/sec scales near-linearly with B). The 📐-2 benchmark is unusually rigorous: it allocates REAL KV-cache tensors at T ∈ {2048, 8192, 32768, 131072} bf16 for a LLaMA-3-8B-shaped config (L=32, n_kv_heads=8, d_head=128), measures the allocated memory via `mx.get_peak_memory()` (with legacy `mx.metal.get_peak_memory()` fallback), prints the analytic formula result alongside, and shows that empirical allocation matches `2·L·T·n_kv_heads·d_head·2` EXACTLY to the byte. Both benchmarks wrap final outputs in `mx.eval`. All module-level variables are underscore-prefixed (`_L`, `_T`, `_n_kv`, `_d_head`, `_B`, `_cache`, `_times`, `_ms_per_tok`) — continuing the nb05/nb06 collision-avoidance discipline. Ceiling: 📐 tables lack an explicit "Bottleneck" classification row (prefill is compute-bound; decode is memory-bandwidth-bound — the whole POINT of the chapter but not tabulated); 📐-1 benchmark could also report derived GB/s HBM bandwidth to empirically show decode hits 60-90% of peak bandwidth. The 📐-2 benchmark could additionally walk the MLA rank-r projection's memory saving to show ~10× compression vs GQA. |
| Frontier Currency | 9/10 | 🔭 Frontier Context cell cites FIVE works across 2023–2026, all directly on-topic for decode-time acceleration: speculative decoding (Leviathan/Chen 2023, arxiv 2211.17192) with the rejection-sampling lossless-ness proof; Medusa (Cai et al. 2024, arxiv 2401.10774) with the extra-decoder-heads tree-verification mechanism; EAGLE-2 (Li et al. 2024, arxiv 2406.16858) with the next-hidden-state prediction for ~80% acceptance and the production default in SGLang; Kangaroo (2024) with early-exit dynamic compute allocation; vLLM v1 (2025 design doc) packaging chunked prefill + continuous batching + prefix cache + spec-decode for 4-8× cumulative throughput vs 2023 naive serving. Four of five citations are 2024+ flagship techniques; vLLM v1 is the 2025 production frontier. Current-SOTA paragraph explicitly names the late-2025 default stack (vLLM v1 / SGLang with EAGLE-2 spec-decode + RadixAttention prefix cache + chunked prefill) AND the open problem (reasoning models with high-entropy CoT collapse spec-decode acceptance rates). Ceiling: no direct late-2025 flagship citation (Qwen-3 inference stack, LLaMA-3.3 serving) beyond the pointer in current-SOTA; MLA citation (DeepSeek-V3) is mentioned in q03 but not pinned to a specific arxiv ID in the 🔭 list itself. |
| Production Anchoring | 8/10 | 🏭 Production Context cell references all four of vLLM, SGLang, TensorRT-LLM, and MLX-LM (Requirement 2.5 floor is 3 ✓). Each row captures a concrete mechanism AND a production-specific detail — vLLM's PagedAttention with block_size=16 and `enable_prefix_caching` as the 2024 chat-serving default; SGLang's RadixAttention for tree-structured prefix-cache reuse across sibling requests (ideal for agentic / tool-use workloads); TensorRT-LLM's engine-build-time fusion (`GemmPlugin` + specialized kernels per (d_model, dtype)) with FP8 decode on H100/H200; MLX-LM's `mx.fast.scaled_dot_product_attention` with UMA-backed zero-copy KV-cache residency on M-series, exploiting the fact that cache and compute share the same DRAM pool. Interview tip present in the template footer. Same 8/10 ceiling as prior notebooks: no per-row throughput figure (no tokens/sec at (B=16, T=8k) for vLLM/SGLang/TRT-LLM, no concrete TTFT delta between chunked-prefill and unchunked paths) — the MLX-LM UMA angle is well-argued but could use a measured M4 Pro decode ms/token number at T=2048 bf16. |
| Failure-Mode Literacy | 8/10 | 🛠️ Failure Modes & Debugging cell covers THREE production-class failure modes, each with an explicit reproducer → assertion pattern: (a) **repetition_penalty applied AFTER softmax** — builds correct (apply to logits, then softmax) vs buggy (softmax, then divide probs by penalty, renormalize) paths at vocab=8, asserts the buggy path breaks the simplex and assigns non-monotone probability to previously-seen tokens; (b) **KV-cache position-id off-by-one on prefix-cache reuse** — builds a 16-token prefix and a 4-token suffix, asserts that using position-ids (0..19) for the combined sequence gives a DIFFERENT output than using (0..15) for prefix and (0..3) for suffix — demonstrates the silent-wrong bug where prefix caching "works" but produces garbage because position-ids restart; (c) **temperature applied to softmax outputs instead of logits** — builds correct (logits/T → softmax) vs buggy (logits → softmax → divide probs by T → renormalize) at T=2.0, asserts the two distributions differ materially and that the buggy path does NOT actually flatten the distribution (it just rescales). Three distinct root causes × three distinct assertion-backed reproducers. Conceptually paired with q01 (temperature derivation), q04 (repetition penalty application), and q06 (prefix-cache position-ids). Ceiling: same as nb02/nb03/nb04/nb05/nb06 — folding three symptoms into ONE 🛠️ cell hurts scanability; splitting into three distinct 🛠️ anchors (each with its own heading) would earn 9. One additional failure mode — e.g., "EOS token not seen during prefill → generation never terminates because the model's learned EOS-probability distribution gets no gradient signal" — would saturate. |

## Strengths
- Full interview-layer stack present and template-consistent: 🎯 🧑‍💻 📐 🏭 🔭 🛠️ prefixes, `---` separators, and a `📋 Interview Question Index` tail cell — idempotent re-run markers (`🎯 Interview Question nb07-q` and `📋 Interview Question Index`) are both present (Requirement 10.1). Re-running the transform is a safe no-op.
- Seven interview questions cover every mandated focus topic for LLD-4 (temperature derivation, generation-strategy comparison, KV-cache memory math, repetition-control mechanisms, prefill-vs-decode asymmetry, TTFT math + RAG serving, decode-acceleration frontier) plus one dedicated research-tier Q (q07) that ties speculative decoding + Medusa + EAGLE-2 + Kangaroo + vLLM v1 under the "decode is memory-bandwidth-bound" common theme — no artificial padding.
- Whiteboard-A (`top-p sampling from scratch`) implements nucleus sampling using the exact MLX primitives a production kernel would use (sort-descending, cumulative-sum, nucleus-mask, renormalize, multinomial), then verifies three independent correctness properties: (a) larger p → same-or-wider nucleus (monotonicity in p), (b) tokens OUTSIDE the nucleus have exactly zero probability mass, (c) p=1 reduces to plain temperature sampling (assertion against `softmax(logits / T)`). Three independent correctness properties from one implementation.
- Whiteboard-B (KV-cache memory) allocates a REAL cache tensor at LLaMA-3-8B shape (L=32, T=8192, n_kv_heads=8, d_head=128) bf16, asserts the allocated byte count EXACTLY equals `2 · L · T · n_kv_heads · d_head · 2 = 1,073,741,824` bytes = 1 GiB, AND walks the hypothetical MHA (n_kv_heads=32) case to show the factor-of-4 GQA saving. Two assertions on the same allocation.
- The 📐-1 benchmark's ms/token column does a job most decode benchmarks omit: it prints per-step decode latency AND tokens/sec at increasing batch sizes (B ∈ {1, 4, 16}), letting the student see the decode-batch-scaling property directly — ms/token stays ~flat while tokens/sec scales near-linearly with B, empirically confirming the bandwidth-bound hypothesis from q05.
- The 📐-2 benchmark handles MLX API churn gracefully — uses `getattr` to fall back to `mx.metal.get_peak_memory` / `mx.metal.reset_peak_memory` for older builds while preferring the new top-level `mx.get_peak_memory` / `mx.reset_peak_memory` — so the cell runs green across MLX versions without emitting deprecation warnings during notebook execution.
- Every variable in every inserted code cell is underscore-prefixed (`_L`, `_T`, `_n_kv`, `_d_head`, `_B`, `_d_model`, `_cache`, `_times`, `_ms_per_tok`, `_toks_per_sec`, `_logits`, `_probs`, `_sorted_idx`, `_cumsum`, `_nucleus_mask`) to avoid collision with the notebook's pre-existing `x, model, block, config` globals defined in the original cells. Systematic engineering discipline carried over from nb05/nb06 — one stale `cache` or `logits` from a previous cell would silently break a whiteboard assertion downstream.
- Three pre-existing bugs in the original notebook were FIXED during transformation (these are included in the commit as narrative preservation corrections rather than interview-layer additions): (a) `mx.utils.tree_flatten` → `mlx.utils.tree_flatten` in the generate-function cell (the `mx.utils` alias was removed upstream); (b) `data[i:...]` with `i` as an MLX array was failing "slice indices must be integers" → wrapped as `data[int(i):...]` via a helper that unwraps MLX scalars, same fix ALSO applied to `utils/training.py` which has the same bug; (c) a benchmark call was passing only `block(x)` without the required `mask=None` kwarg → corrected to `block(x, mask=None)`. All three are minimal, local, preserve the narrative intent exactly.
- The 🛠️ cell reproduces three distinct production-class generation bugs: repetition_penalty applied AFTER softmax (breaks the simplex; reproducible via assertion that the post-softmax rescale does NOT reduce to the pre-softmax divide), KV-cache position-id off-by-one on prefix-cache reuse (silent-wrong: cache "works" but output differs from the uncached baseline), temperature applied to softmax outputs instead of logits (buggy path does NOT flatten the distribution, it merely rescales — assertion against the correct path's entropy).
- Every question has populated `trap` + `references` fields with authoritative sources (OpenAI sampling guide, Holtzman nucleus-sampling, Keskar CTRL, LLaMA-3, GQA, PagedAttention, vLLM v1, Leviathan spec-decode, Medusa, EAGLE-2, Kangaroo, DoLa, Flash-Decoding, SGLang blog, diverse-beam); role × tier coverage is dense enough that `systems_engineer` filters 5 of 7 questions and `research_engineer` filters 5 of 7.
- Pre-existing narrative cells (Environment Validation, Big Picture, Library Imports, GPT assembly, Generation loop, KV-cache demo, Sampling-strategy demo, History & Alternatives, Summary) are untouched — the interview layer is purely additive (Requirement 6.1), and the `_insert_block` / bottom-up anchor-resolution logic in the (MCP-driven) insertion path preserves idempotency on re-run (Requirement 10.2–10.4, verified by re-executing the transform and observing zero new cells).
- End-to-end notebook execution passes: `jupyter nbconvert --to notebook --execute --inplace` completes on the 77-cell transformed notebook with zero errors; full pytest suite green at 223 passed (no regressions vs the pre-nb07 223 baseline).

## Weaknesses
- **Failure-Mode Literacy (8/10):** three symptoms folded into ONE 🛠️ cell. Splitting them into three separate 🛠️ anchors (each with its own reproducer + fix + assertion and its own heading) would hit 9. Same density-vs-scanability trade-off as nb02/nb03/nb04/nb05/nb06. One additional failure mode — EOS token not seen during prefill → generation never terminates — is mentioned implicitly in q06 but not reproduced as its own symptom.
- **Production Anchoring (8/10):** no per-row throughput figure (no tokens/sec at (B=16, T=8k) for vLLM/SGLang/TRT-LLM); no concrete TTFT delta in ms between chunked-prefill and unchunked paths. The MLX-LM UMA angle is well-argued but could use a measured M4 Pro decode ms/token number at T=2048 bf16.
- **Systems Rigor (9/10):** 📐 tables lack an explicit "Bottleneck" classification row (prefill is compute-bound; decode is memory-bandwidth-bound — the whole POINT of the chapter but not tabulated); 📐-1 benchmark could also report derived GB/s HBM bandwidth to empirically show decode hits 60-90% of peak bandwidth. 📐-2 could walk the MLA rank-r projection math to show ~10× compression vs GQA.
- **Frontier Currency (9/10):** no direct late-2025 flagship citation (Qwen-3 inference stack, LLaMA-3.3 serving) beyond the pointer in current-SOTA; MLA (DeepSeek-V3) is referenced in q03 but not pinned to a specific arxiv ID in the 🔭 list.
- **Technical Depth (9/10):** q03's MLA coverage could walk the rank-r projection math explicitly (DeepSeek-V3's ~10× compression vs GQA via low-rank latent); q07's vLLM v1 coverage could derive the chunked-prefill scheduling-policy trade-off (chunk size C picks point on the TTFT-vs-throughput Pareto frontier).
- **Interview Breadth (9/10):** only one research-tier Q (q07) but it bundles five compositional decode-acceleration techniques — feels like five micro-Qs. One more independent research-tier Q (e.g., "derive MLA's rank-r latent decomposition from the KV-cache-minimizing objective" or "prove speculative decoding's output distribution matches the target's via rejection sampling") would saturate.

## Required fixes (if FAIL)
Total 52/60 is above the 48/60 bar — **no blocking fixes required**. The following are optional tightening edits queued for Task 27 rework:

1. **Split the 🛠️ cell into three separate debugging cells** (repetition_penalty after softmax / KV-cache position-id off-by-one / temperature after softmax). Each gets its own reproduce-then-fix pairing and its own 🛠️ anchor heading; add a 4th failure mode showing "EOS token never produced → generation loops until max_tokens" with the fix (sample EOS with temperature=0 after N tokens, or penalize non-EOS near the length limit).
2. **Add a "Bottleneck" classification row** to both 📐 complexity tables (prefill: compute-bound; decode: memory-bandwidth-bound) and extend the 📐-1 benchmark to report derived GB/s HBM bandwidth so students can see decode hits 60-90% of peak bandwidth.
3. **Add one more research-tier Q** — suggested: "Derive MLA's rank-r latent decomposition (DeepSeek-V3): given the KV-cache-minimizing objective over rank r, what r delivers ~10× compression vs GQA at (L=61, d_model=7168, n_kv_heads=128)?" (research_engineer).
4. **Add one concrete throughput figure per 🏭 row** (e.g., "vLLM v1 on H100 at (B=16, T=8k, LLaMA-3-8B) bf16: ~X tokens/sec; SGLang: ~Y; TRT-LLM: ~Z; MLX-LM `mx.fast.scaled_dot_product_attention` on M4 Pro: ~W").
5. **Pin MLA to its arxiv paper** (DeepSeek-V2 technical report 2405.04434) in the 🔭 list and add one late-2025 flagship model citation to the 🔭 cell (Qwen-3 or LLaMA-3.3 inference stack) so the frontier list has two 2025 references.
6. **Saturate q03** with the explicit MLA rank-r projection derivation from the KV-cache-minimizing objective, showing the ~10× compression at DeepSeek-V3's L=61, d_model=7168, n_kv_heads=128 shape.
7. **Saturate q07** with the explicit chunked-prefill Pareto-frontier derivation: chunk size C trades TTFT (lower C → faster first token) for throughput (higher C → amortized prefill), and the optimal C depends on target SLO.

Projected post-fix total: 10 + 10 + 10 + 10 + 9 + 9 = **58/60**.

## Status
PASS — committed as the nb07 interview-grade transform. Not blocking for subsequent wave-A notebooks (nb08 training, nb09 modern architectures).
