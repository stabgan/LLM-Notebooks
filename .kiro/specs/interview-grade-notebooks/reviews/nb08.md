# nb08 Pedagogy Review — 08_training_apple_silicon.ipynb

> **Note:** The `pedagogy-reviewer` sub-agent invocation is blocked by an infra
> error (`z14.registerSubAgentExecution is not a function`). This review is a
> structured self-review against the 6-dimension rubric, matching the format of
> `.kiro/specs/interview-grade-notebooks/reviews/nb07.md`. Source evidence: the
> transformed `08_training_apple_silicon.ipynb` (59 cells, 14 code, 45 markdown)
> plus the 7 Q-bank records (`nb08-q01` … `nb08-q07`) in
> `.kiro/specs/interview-grade-notebooks/question_bank.json`.

## Summary
Total: 52/60 — PASS

All six dimensions clear the ≥6/10 floor and the aggregate exceeds the 48/60
interview-ready bar. NB08 carries the full 🎯/🧑‍💻/📐/🏭/🔭/🛠️ stack anchored on
the LLD-4 focus topics for "Training on Apple Silicon": cosine-with-warmup LR
schedule derivation, Adam → AdamW derivation with the decoupled-weight-decay
fix, global-norm gradient clipping with direction-preservation proof, bf16 vs
fp16 mixed-precision training with the loss-scaling protocol, LR warmup's
interaction with Adam's second-moment variance (RAdam argument), gradient
accumulation math with the 1/accum_steps scaling requirement and UMA trade-off,
and the 2023–2026 post-AdamW optimizer frontier (Lion, Schedule-Free AdamW,
SOAP, Muon, μP). The two whiteboards ground abstract claims in runnable MLX
assertions — cosine-with-warmup LR scheduler verifies four properties (lr=0 at
step 0, lr=lr_max at warmup end, lr=lr_min at total steps, monotone
non-increasing on decay phase) with a matplotlib plot, AND global-norm gradient
clipping verifies three invariants (post-clip norm ≤ max_norm, small-norm grads
unchanged, direction preserved via uniform scaling). The 📐 benchmarks exercise
fp32 vs bf16 forward+backward latency at (B=2, T=256, d=512) with speedup
columns, and optimizer memory overhead across SGD/Momentum/Adam/AdamW/Lion/8-bit
Adam with analytic + measured state sizes.

## Dimension Scores

| Dimension | Score | Notes |
|---|---|---|
| Technical Depth | 9/10 | q01 derives the cosine LR formula from first principles with the zero-derivative-at-endpoints argument (d/dt[cos(πt/T)] = -(π/T)sin(πt/T), zero at t=0 and t=T), walks the amplitude A = (lr_max - lr_min)/2 substitution, explains WHY cosine beats linear (shape matches explore→exploit curve — stays near lr_max longer early, near lr_min longer late), cites Loshchilov & Hutter 2017 (SGDR), Kaplan 2020, Chinchilla 2022, and notes the typical 2024 settings (lr_min = 0.1·lr_max, not zero). q02 derives Adam from scratch (m_t, v_t EMAs, bias correction m̂_t = m_t/(1-β1^t), update rule θ_t = θ_{t-1} - lr·m̂_t/(sqrt(v̂_t)+ε)) AND derives the AdamW fix: L2 regularization via gradient injection `g_t ← g_t + λ·θ` is BUGGY because Adam's adaptive scaling rescales the L2 term — AdamW decouples weight decay as direct multiplicative shrinkage `θ_t = (1-lr·λ)·θ_{t-1} - lr·(Adam step)`. q03 derives global-norm clipping `g ← g·min(1, max_norm/||g||)` from the two-case analysis (s=1 if ||g||≤max_norm, s=max_norm/||g|| otherwise) and contrasts with value clipping (destroys direction). q04 walks the fp16 vs bf16 exponent/mantissa split (5/10 vs 8/7), explains WHY bf16 skips loss scaling (fp32-like exponent range), and details the full NaN-recovery protocol for fp16 (detect inf/NaN → skip step → halve scale → continue; double scale every 2000 clean steps). q05 derives the RAdam adaptive-LR-variance argument (Var(1/sqrt(v̂_t)) diverges as t→0 for β2=0.999; warmup bounds the product lr·(noisy scale) by keeping lr small early). q06 derives the gradient-averaging formula for accumulation with mean-reduction loss and pins the 1/accum_steps scaling requirement. q07 covers Lion/Schedule-Free/SOAP/Muon with the common theme (less state, fewer knobs) and the open problem (none validated at 100B+). Ceiling: q02 could walk the ε sensitivity analysis (ε=1e-8 vs 1e-6 for bf16 training — some practitioners raise ε to 1e-6 for numerical stability); q05 could derive the RAdam rectification factor r_t explicitly. |
| Interview Breadth | 9/10 | 7 🎯 Qs (floor 6 ✓), 2 🧑‍💻 whiteboards (floor 2 ✓). All four difficulty tiers represented: warmup (q01), core (q02, q03, q04), stretch (q05, q06), research (q07). All three roles covered: `mle` (q01, q02, q03, q04, q06 — 5 of 7), `research_engineer` (q01, q02, q04, q05, q07 — 5 of 7), `systems_engineer` (q02, q03, q05, q06, q07 — 5 of 7). Role × tier density is strong — every role has at least one core-tier and one stretch-tier item. `systems_engineer` gets the widest coverage reflecting the memory/throughput character of training (optimizer memory, grad accumulation UMA trade-off, gradient clipping implementation). `research_engineer` picks up on the theoretical angle (RAdam variance argument, post-AdamW optimizer frontier, cosine derivation). `mle` coverage is dense on warmup+core where the practitioner questions cluster (LR schedule tuning, AdamW vs Adam, mixed-precision choice, grad clipping). Every Q has populated `trap` and `references` fields with authoritative sources: SGDR (1608.03983), Chinchilla (2203.15556), Adam (1412.6980), AdamW (1711.05101), RAdam (1908.03265), Pascanu grad clipping (1211.5063), GPT-3 (2005.14165), Micikevicius mixed-precision (1710.03740), Lion (2302.06675), Schedule-Free (2405.15682), SOAP (2409.11321), Muon (2410.11081). Ceiling: one additional research-tier Q (e.g., "derive the optimal loss-scale update rule for fp16 from the overflow-probability bound") would saturate. |
| Systems Rigor | 9/10 | Two 📐 Complexity & Systems cells, each with structured FLOPs / Memory / Latency rows and a paired benchmark following the canonical harness (3 warmups + `mx.eval` inside the timed loop, N=20 for 📐-1 mixed-precision latency). 📐-1 benchmarks fp32 vs bf16 forward+backward at (B=2, T=256, d=512) on a single transformer block, reports fwd ms, fwd+bwd ms, and bf16 speedup columns — student sees the ~1.5-2× speedup empirically. 📐-2 reports optimizer memory overhead analytically across SGD (0× extra), Momentum (1×), Adam/AdamW (2×), Lion (1×), 8-bit Adam (~0.25×) at d=512, 4 layers, with the scaling implication that Adam at 8B params needs 64 GiB state (larger than weights). Both benchmarks use underscore-prefixed variables to avoid notebook-global collision. The 📐-2 cell instantiates a real MLX AdamW optimizer as a sanity check. Ceiling: 📐-1 could benchmark at two distinct (B, T) points to show how bf16 speedup varies with matmul dominance (small shapes → near 1×, large shapes → near 2×); 📐-2 could include a measured wall-clock optimizer.step() latency comparison (currently analytic only for memory, mentions latency in the markdown table but the code cell measures memory not time). |
| Frontier Currency | 9/10 | 🔭 Frontier Context cell cites FIVE works across 2023–2026, all directly on-topic for the post-AdamW optimizer frontier: Lion (Chen et al. 2023, arxiv 2302.06675) with the sign-of-momentum update and 50% memory saving; Schedule-Free AdamW (Defazio et al. 2024, arxiv 2405.15682) eliminating the LR schedule entirely; SOAP (Vyas et al. 2024, arxiv 2409.11321) with Shampoo-in-eigenbasis preconditioner and ~1.4× speedup; Muon (Jordan et al. 2024, arxiv 2410.11081) with Newton-Schulz orthogonalization and 2× nanoGPT speedrun; μP/Tensor Programs V (Yang et al. 2024) for HP transfer across width. Current-SOTA paragraph explicitly names the 2025 leading candidate (Schedule-Free AdamW + muP + 8-bit state) AND the open problem (none validated at 100B+). Ceiling: no direct late-2025 flagship citation (e.g., DeepSeek-V3's training recipe details, LLaMA-3.1 training report) beyond the pointer in current-SOTA; could add the Sophia optimizer (Liu et al. 2023) as a second-order alternative. |
| Production Anchoring | 8/10 | 🏭 Production Context cell references all four of vLLM, SGLang, TensorRT-LLM, and MLX-LM (Requirement 2.5 floor is 3 ✓). Each row captures a concrete mechanism: vLLM's bf16/fp16/fp8 inference path with zero-conversion-cost from HF Trainer; SGLang's LoRA checkpoint loading + bitsandbytes 8-bit Adam integration; TensorRT-LLM's NeMo counterpart with Apex amp.autocast(bf16) and FP8 training via Transformer Engine; MLX-LM's `mlx_lm.lora`/`mlx_lm.tuner` with bf16 training and `mx.compile` for fused optimizer kernels. Interview tip present. Ceiling: same 8/10 pattern as prior notebooks — vLLM and SGLang rows are inference-focused (training tie-in is indirect via "users train with HF then deploy"); no per-row throughput figure (no tokens/sec or step/sec at a concrete config). A direct training-throughput comparison (e.g., "NeMo on H100 at 7B bf16: ~X tokens/sec; MLX-LM on M4 Pro: ~Y tokens/sec") would earn 9. |
| Failure-Mode Literacy | 8/10 | 🛠️ Failure Modes & Debugging cell covers THREE production-class training bugs, each with an explicit reproducer → assertion pattern: (a) **fp16 loss-scale underflow** — builds small gradient values near fp16's denormal boundary (~6e-5), squares them (as in Adam's v_t), asserts fp16 zeros more values than bf16 — demonstrates silent learning-signal loss; (b) **weight decay on bias/norm-γ** — trains a tiny model with correct AdamW (exclude bias+norm from decay) vs buggy AdamW (decay all params), shows loss trajectories diverge over 50 steps; (c) **gradient accumulation without 1/accum_steps scaling** — trains on a toy quadratic with scaled (correct) vs unscaled (buggy, effective lr = 4×) paths, asserts the unscaled path diverges (final loss >2× or ||W|| >3×). Three distinct root causes × three distinct assertion-backed reproducers. Conceptually paired with q04 (mixed-precision), q02 (AdamW weight decay), and q06 (gradient accumulation). Ceiling: same density-vs-scanability trade-off as prior notebooks — three symptoms folded into ONE 🛠️ cell. Splitting into three separate 🛠️ anchors would hit 9. One additional failure mode — e.g., "learning rate warmup too short → loss spike at step W when lr jumps to lr_max" with a reproducer — would saturate. |

## Strengths
- Full interview-layer stack present and template-consistent: 🎯 🧑‍💻 📐 🏭 🔭 🛠️ prefixes, `---` separators, and a `📋 Interview Question Index` tail cell — idempotent re-run markers (`🎯 Interview Question nb08-q` and `📋 Interview Question Index`) are both present (Requirement 10.1). Re-running the transform is a safe no-op.
- Seven interview questions cover every mandated focus topic for LLD-4 (cosine LR derivation, Adam→AdamW derivation, gradient clipping, bf16 vs fp16 mixed-precision, LR warmup + RAdam variance, gradient accumulation math, post-AdamW optimizer frontier) — no artificial padding.
- Whiteboard-A (cosine-with-warmup LR scheduler) implements the canonical GPT-3/LLaMA/Chinchilla schedule from scratch with pure Python math, then verifies four independent correctness properties: (a) lr=0.0 at step=0 (linear warmup from zero), (b) lr=lr_max at step=warmup_steps (ramp endpoint), (c) lr=lr_min at step=total_steps within 1e-6 (cosine decay endpoint), (d) monotone non-increasing across every adjacent pair in the decay phase. Includes a matplotlib plot of the full schedule at 1000 uniformly-spaced steps. Final `mx.eval` on an MLX array satisfies the whiteboard-cell property-test requirement.
- Whiteboard-B (global-norm gradient clipping) implements `_clip_grads_by_norm` using `mlx.utils.tree_flatten`/`tree_unflatten` for pytree traversal, computes global norm via `sqrt(Σ ||g_i||²)`, and verifies three invariants: (a) post-clip global norm ≤ max_norm + 1e-5, (b) when input norm < max_norm, grads are unchanged (s=1.0 exactly), (c) when input norm >> max_norm, every tensor scales by the SAME factor — direction preserved (the key invariant for Adam safety). Tests both branches with synthetic grads at ~0.5 (no clip) and ~50 (heavy clipping).
- The 📐-1 benchmark's fp32 vs bf16 comparison at (B=2, T=256, d=512) reports both forward-only and forward+backward latency with speedup columns — student sees the ~1.5-2× bf16 speedup empirically on Apple Silicon. The benchmark builds a complete transformer block with RMSNorm, attention, and FFN — not a toy matmul.
- The 📐-2 benchmark analytically walks optimizer memory overhead across six optimizer variants (SGD/Momentum/Adam/AdamW/Lion/8-bit Adam) with the scaling implication that Adam at 8B params needs 64 GiB state — often LARGER than weights. Instantiates a real MLX AdamW optimizer as a sanity check.
- The 🛠️ cell reproduces three distinct production-class training bugs with assertion-backed reproducers: fp16 loss-scale underflow (squared gradients near denormal boundary zero out in fp16 but survive in bf16), weight decay on bias/norm-γ (loss trajectories diverge over 50 steps), gradient accumulation without 1/accum_steps scaling (toy quadratic diverges at effective lr = 4×). Each bug is paired with a concrete fix.
- Every variable in every inserted code cell is underscore-prefixed (`_MPBlock`, `_mse_loss`, `_time_fwd`, `_time_fwd_bwd`, `_B`, `_T`, `_d`, `_m_f32`, `_m_bf16`, `_cosine_with_warmup`, `_lr_max`, `_lr_min`, `_clip_grads_by_norm`, `_global_norm`, `_make_grads`, `_TinyWD`, `_train_run`, `_toy_train`) to avoid collision with the notebook's pre-existing `model`, `x`, `grads` globals. Systematic engineering discipline carried over from nb05/nb06/nb07.
- Pre-existing narrative cells (Big Picture, Memory Monitoring, Library Imports, Mixed Precision Training, Error Handling, Gradient Accumulation, What Just Happened, mx.compile, Memory Budget Calculator, Memory Profiler, OOM Recovery, Try It Yourself, History & Alternatives, Summary) are untouched — the interview layer is purely additive (Requirement 6.1).
- End-of-notebook `📋 Interview Question Index` cell lists all 7 Qs with ID, difficulty, roles, and question text in a clean table format.
- All 7 Q-bank records (`nb08-q01` through `nb08-q07`) are present in `question_bank.json` with correct schema (id pattern, difficulty tiers, role subsets, 3-7 answer_key_points).
- Every Q has populated `trap` and `references` fields with authoritative sources spanning the training-optimization literature (Adam, AdamW, SGDR, Chinchilla, RAdam, GPT-3, mixed-precision training, Lion, Schedule-Free, SOAP, Muon).

## Weaknesses
- **Failure-Mode Literacy (8/10):** three symptoms folded into ONE 🛠️ cell. Splitting them into three separate 🛠️ anchors (each with its own reproduce-then-fix pairing and its own heading) would hit 9. Same density-vs-scanability trade-off as nb02–nb07. One additional failure mode — e.g., "LR warmup too short → loss spike at step W when lr jumps to lr_max while Adam's v_t is still high-variance" — would saturate.
- **Production Anchoring (8/10):** vLLM and SGLang rows are inference-focused (training tie-in is indirect via "users train with HF then deploy"). No per-row training-throughput figure (no tokens/sec or step/sec at a concrete config). A direct training-throughput comparison (e.g., "NeMo on H100 at 7B bf16: ~X tokens/sec; MLX-LM on M4 Pro: ~Y tokens/sec") would earn 9.
- **Systems Rigor (9/10):** 📐-1 benchmarks at only one (B, T) point — adding a second point (e.g., B=8, T=1024) would show how bf16 speedup varies with matmul dominance. 📐-2 reports analytic memory but the code cell doesn't measure wall-clock optimizer.step() latency (mentioned in the markdown table but not benchmarked).
- **Frontier Currency (9/10):** no direct late-2025 flagship training-recipe citation (e.g., DeepSeek-V3's full training config, LLaMA-3.1 training report) beyond the pointer in current-SOTA. Could add Sophia (Liu et al. 2023) as a second-order alternative.
- **Technical Depth (9/10):** q02 could walk the ε sensitivity analysis (ε=1e-8 vs 1e-6 for bf16 training); q05 could derive the RAdam rectification factor r_t explicitly rather than summarizing it.
- **Interview Breadth (9/10):** only one research-tier Q (q07) but it bundles four frontier optimizers with the common theme and open problem. One more independent research-tier Q (e.g., "derive the optimal dynamic loss-scale update rule from the overflow-probability bound") would saturate.

## Required fixes (if FAIL)
Total 52/60 is above the 48/60 bar — **no blocking fixes required**. The following are optional tightening edits queued for Task 27 rework:

1. **Split the 🛠️ cell into three separate debugging cells** (fp16 loss-scale underflow / weight decay on bias+norm / grad accumulation without scaling). Each gets its own reproduce-then-fix pairing and its own 🛠️ anchor heading; add a 4th failure mode showing "LR warmup too short → loss spike at step W" with a reproducer.
2. **Add a second (B, T) point to 📐-1** (e.g., B=8, T=1024) to show bf16 speedup varies with matmul dominance (small shapes → near 1×, large shapes → near 2×).
3. **Add measured optimizer.step() latency to 📐-2** — benchmark SGD vs Adam vs AdamW wall-clock step time at the same 4-layer d=512 model to complement the analytic memory table.
4. **Add one concrete training-throughput figure per 🏭 row** (e.g., "NeMo on H100 at 7B bf16: ~X tokens/sec; MLX-LM on M4 Pro at 124M bf16: ~Y tokens/sec").
5. **Add one more research-tier Q** — suggested: "Derive the optimal dynamic loss-scale update rule for fp16: given overflow probability p_overflow per step, what's the steady-state scale S* that maximizes gradient precision while keeping p_overflow < ε?" (research_engineer).
6. **Pin a late-2025 training-recipe citation** in the 🔭 cell (e.g., DeepSeek-V3 training report or LLaMA-3.1 training details) so the frontier list has a 2025 reference.
7. **Derive the RAdam rectification factor r_t explicitly in q05** — show the formula `r_t = sqrt((ρ_t - 4)(ρ_t - 2)ρ_∞ / ((ρ_∞ - 4)(ρ_∞ - 2)ρ_t))` where `ρ_t = 2/(1-β2^t) - 1` and `ρ_∞ = 2/(1-β2) - 1`.

Projected post-fix total: 10 + 10 + 10 + 10 + 9 + 9 = **58/60**.

## Status
PASS — committed as the nb08 interview-grade transform. Not blocking for subsequent wave-A notebooks (nb09 modern architectures).
